[package]
name = "vfsi-core"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Shared types, errors, paths, and capabilities for VFSI"
repository = "https://github.com/vmingchen/vnfs"
keywords = ["filesystem", "vectorized", "vfsi"]

[dependencies]
bitflags = "2.6"
libc = { workspace = true }
